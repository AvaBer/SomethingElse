package somethingelse.position;

public class PlayerPosition {
    private Position currentPosition;
    private Position oldPosition;

    public PlayerPosition(Position currentPosition) {
        this.currentPosition = currentPosition;
    }
}
